--- a/aimaos/app/streamlit_app.py
+++ b/aimaos/app/streamlit_app.py
@@ -1781,7 +1781,10 @@ def render_sample_analysis_preview(view_mode: str) -> None:
         return
 
     if sample_mode == "demo":
-        st.info(DEMO_DATA_NOTICE)
+        if using_demo_data():
+            st.info(DEMO_DATA_NOTICE)
+        else:
+            st.caption("현재 분석에는 업로드 데이터가 우선 적용됩니다. 아래 데이터는 체험용으로만 사용됩니다.")
         preview_advertiser = "샘플 데모 광고주"
         report_folder = "demo_streamlit"
         button_label = "데모 데이터 분석 실행"
