name: Apply marketplace row count logging

on:
  push:
    paths:
      - .github/patches/marketplace-rows.zip.b64
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Decode and apply patch
        shell: bash
        run: |
          base64 --decode .github/patches/marketplace-rows.zip.b64 > /tmp/marketplace-rows.zip
          unzip -p /tmp/marketplace-rows.zip > /tmp/marketplace-rows.patch
          git apply --check /tmp/marketplace-rows.patch
          git apply /tmp/marketplace-rows.patch
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements-dev.txt
      - run: python -m compileall aimaos tests
      - run: python -m pytest tests -q
      - name: Commit verified changes
        shell: bash
        run: |
          git config user.name "CampaignPulse CI"
          git config user.email "actions@users.noreply.github.com"
          git add aimaos/collectors/marketplace/base_collector.py aimaos/collectors/marketplace/marketplace_collection_poc.py tests/test_marketplace_collection.py docs/COLLECTION_LOG_SCHEMA.md
          git commit -m "Preserve marketplace collection row counts"
          git push origin HEAD:main
